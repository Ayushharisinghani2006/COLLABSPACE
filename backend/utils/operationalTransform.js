// Placeholder for operational transformation logic (used in collaborative editing)
const applyOperation = (state, operation) => {
  // Example: Update whiteboard elements
  return { ...state, ...operation };
};

module.exports = { applyOperation };